### Array 모두 더하기
```
function solution(n)
{
	const str = String(n);
	const newArr = Array.from(str);
    let plus
    for(let i = 0; i < newArr.length; i++){
    if(!plus){
        console.log("ff")
        plus = Number(newArr[i])
    } else {
        plus += Number(newArr[i])
    }
    }
 
    
   return plus
}

solution(1243)
```

</br>

### x만큼 간격이 있는 n개의 숫자 
```
function solution(x, n) {
    let answer = []
    for(let i = 1; i <= n; i++){
        answer.push(x * i)
    }
    return answer;
}
```
